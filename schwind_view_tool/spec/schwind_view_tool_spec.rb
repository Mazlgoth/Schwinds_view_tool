require "spec_helper"

describe SchwindViewTool do
  it "has a version number" do
    expect(SchwindViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
